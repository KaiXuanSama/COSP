spring:
  application:
    name: copilot-ollama-proxy
  config:
    activate:
      on-profile: docker

server:
  port: ${SERVER_PORT:11434}

ollama:
  version: ${OLLAMA_VERSION:0.6.4}

mimo:
  api-key: ${MIMO_API_KEY:}
  base-url: ${MIMO_BASE_URL:https://api.xiaomimimo.com/anthropic}
  default-model: ${MIMO_DEFAULT_MODEL:mimo-v2.5-pro}